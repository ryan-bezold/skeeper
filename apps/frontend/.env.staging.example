# =============================================================================
# Skeeper Frontend — Staging Environment Variables
# =============================================================================
# Copy to .env.staging and update the URLs to your staging server.

VITE_API_URL=https://api.staging.skeeper.example.com
VITE_WS_URL=wss://api.staging.skeeper.example.com
VITE_ENV=staging
